# this CUPTI stuff is seen in my TensorFlow scripts, so I add it here. But seems not useful though.
export LD_LIBRARY_PATH=${HOME}/cuda-9.0/lib64:${HOME}/cuda-9.0/extras/CUPTI/lib64:$LD_LIBRARY_PATH
export CUDA_HOME=${HOME}/cuda-9.0
